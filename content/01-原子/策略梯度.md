---
tags:
  - atomic
  - policy-gradient
  - foundational
created: 2026-06-22
---

# 策略梯度

> 直接优化策略参数，而不是学习价值函数。核心思想：好的动作应该增加其被选择的概率。

---

## 核心思想

**策略参数化**：将策略表示为参数化函数 $\pi_\theta(a|s)$，其中 $\theta$ 是神经网络参数。

**目标**：找到最优参数 $\theta^*$，使得策略的期望回报最大：

$$J(\theta) = \mathbb{E}_{\pi_\theta}[G_0]$$

**策略梯度定理**：

$$\nabla_\theta J(\theta) = \mathbb{E}_{\pi_\theta}\left[\sum_t \nabla_\theta \log \pi_\theta(a_t|s_t) \cdot G_t\right]$$

**直觉解释**：
- $\nabla_\theta \log \pi_\theta(a_t|s_t)$：如何调整参数以增加动作 $a_t$ 的概率
- $G_t$：这个动作带来的回报
- 如果 $G_t > 0$（好动作），梯度方向会增加 $a_t$ 的概率
- 如果 $G_t < 0$（坏动作），梯度方向会减少 $a_t$ 的概率

---

## 被以下模块使用

- [[02-模块/Policy-Based/REINFORCE]]：最基础的策略梯度算法
- [[02-模块/Policy-Based/PPO]]：策略梯度 + clip机制
- [[02-模块/Policy-Based/TRPO]]：策略梯度 + 信任区域
- [[02-模块/Actor-Critic/A2C]]：策略梯度 + 价值函数基线

---

## 优缺点

### 优点
- ✅ 可以直接学习随机策略（不需要argmax）
- ✅ 适合连续动作空间
- ✅ 可以学习探索策略

### 缺点
- ❌ 高方差（$G_t$ 的估计噪声大）
- ❌ 样本效率低（需要大量轨迹）
- ❌ 收敛慢

---

## 降低方差的方法

1. **基线（Baseline）**：减去状态价值 $V(s_t)$，使用优势函数
   - 详见 [[01-原子/优势函数]]
   
2. **GAE（广义优势估计）**：平衡偏差和方差
   - 详见 [[01-原子/GAE]]

3. **重要性采样**：复用旧数据
   - 详见 [[02-模块/Policy-Based/PPO]]

---

## 演化位置

**在 Policy-Based 演化链中的位置**：

```
策略梯度定理 (1999)
    ↓
REINFORCE (1992) ← **你在这里**
    ↓
Baseline / Advantage (1999)
    ↓
Actor-Critic (2000)
    ↓
TRPO (2015) / PPO (2017)
```

详见 [[03-流程/Policy-AC演化史]]
